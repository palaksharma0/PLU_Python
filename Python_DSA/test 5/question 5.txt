'''
# 5. Movie Recommendation System
### Problem Statement
A streaming platform stores movie information.
Each movie contains
* Movie ID
* Title
* Genre
* Rating
* Watch Count
### Requirements
1. Fetch all movies.
2. Sort movies based on Rating.
3. Search a movie using Movie ID.
4. Display Top 10 highest-rated movies.
5. Display the most watched movie in every genre.
### Concepts
* Sorting
* Searching
* Dictionaries
* SQL GROUP BY
'''



CREATE DATABASE movie_db;
USE movie_db;

CREATE TABLE movies (
    movie_id INT PRIMARY KEY,
    title VARCHAR(100),
    genre VARCHAR(50),
    rating DECIMAL(3,1),
    watch_count INT
);

INSERT INTO movies VALUES
(101,'3 Idiots','Comedy',9.2,5000),
(102,'Dangal','Sports',8.9,4500),
(103,'KGF','Action',8.7,6000),
(104,'Bahubali','Action',9.0,7000),
(105,'PK','Comedy',8.5,5500),
(106,'Chhichhore','Drama',8.8,4000),
(107,'Pushpa','Action',8.4,6500),
(108,'Taare Zameen Par','Drama',9.1,4800),
(109,'Sultan','Sports',8.3,3500),
(110,'Drishyam','Thriller',9.3,5200);

SELECT * FROM movies;

SELECT * FROM movies
ORDER BY rating DESC;

SELECT * FROM movies
WHERE movie_id = 104;

SELECT * FROM movies
ORDER BY rating DESC
LIMIT 10;

SELECT genre, MAX(watch_count) AS highest_watch_count
FROM movies
GROUP BY genre;

SELECT * FROM movies
ORDER BY watch_count DESC
LIMIT 1;
